# AutoWifi
A automatically reconnecting worker service that runs locally for reconnecting to free WIFIs that have limited sessions
<br/>
Currently the worker can only be executed by IDE (VS, VS Code) or through terminal, I'm planning on make an User Interface using .NET WPF framework. 
